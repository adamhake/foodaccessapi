deployment:
  production:
    branch: production
    heroku:
      appname: food-access-api
    commands:
      - "[[ ! -s \"$(git rev-parse --git-dir)/shallow\" ]] || git fetch --unshallow"
  staging:
    branch: master
    heroku:
      appname: food-access-api-stage
